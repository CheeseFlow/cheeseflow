name = "cheeseflow"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Cloudflare Pages configuration
# Deploy with: pnpm run deploy
# Note: Production branch is configured in Cloudflare Pages dashboard
# The deploy command will deploy to the current branch
# Set master/main as production branch in Cloudflare Pages settings

